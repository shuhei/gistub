<div id="gists">
  <%= paginate @gists, :remote => true, :window => 6, :params => {:action => 'page'} %>
  <%= render :partial => 'gists/list' %>
</div>
  <%= paginate @gists, :remote => true, :window => 6, :params => {:action => 'page'} %>
